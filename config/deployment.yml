apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-app-template
  labels:
    app: go-app-template
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-app-template
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: go-app-template
    spec:
      containers:
      - name: go-app-template
        image: <IMAGE>
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: 100m
          limits:
            cpu: 100m
---
apiVersion: v1
kind: Service
metadata:
  name: go-app-template
spec:
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: go-app-template
